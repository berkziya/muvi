// import bcrypt from 'bcryptjs';

// export async function hashIt(password: FormDataEntryValue) {
//   const saltRounds = 10;
//   return bcrypt.hash(password.toString(), saltRounds);
// }

// export async function compareWithHash(
//   password: FormDataEntryValue,
//   hash: string
// ) {
//   return bcrypt.compare(password.toString(), hash);
// }
